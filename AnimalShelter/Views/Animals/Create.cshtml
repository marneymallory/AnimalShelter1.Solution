@{
  Layout = "_Layout";
}
@model AnimalShelter.Models.Animal;

<h4>Add a New Animal</h4>
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Type)
  @Html.TextBoxFor(model => model.Type)
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)
  @Html.LabelFor(model => model.Gender)
  @Html.TextBoxFor(model => model.Gender)
  @Html.LabelFor(model => model.DateOfAdmittance)
  @Html.TextBoxFor(model => model.DateOfAdmittance, new { @class="form-control", type="date"})
  @Html.LabelFor(model => model.Breed)
  @Html.TextBoxFor(model => model.Breed)
  <input type="submit" value="Add New Animal">
}
<p>@Html.ActionLink("Back to All Animals", "Index")</p>